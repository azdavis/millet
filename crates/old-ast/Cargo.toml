[package]
name = "old-ast"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
test = false
doctest = false

[dependencies]
old-intern = { path = "../old-intern" }
old-loc = { path = "../old-loc" }
old-token = { path = "../old-token" }
