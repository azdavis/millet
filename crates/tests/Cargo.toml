[package]
name = "tests"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
doctest = false

[dependencies]
analysis = { path = "../analysis" }
syntax = { path = "../syntax" }
fast-hash = { path = "../fast-hash" }
