[package]
name = "tests"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
doctest = false

[dependencies]
env_logger = { version = "0.9" }
log = "0.4"
once_cell = "1"
pulldown-cmark = "0.9"
config = { path = "../config" }
analysis = { path = "../analysis" }
fast-hash = { path = "../fast-hash" }
syntax = { path = "../syntax" }
paths = { path = "../paths" }
