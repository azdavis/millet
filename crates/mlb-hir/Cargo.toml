[package]
name = "mlb-hir"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
test = false
doctest = false

[dependencies]
text-size-util = { path = "../text-size-util" }
hir = { path = "../hir" }
paths = { path = "../paths" }
