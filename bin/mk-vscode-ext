#!/bin/sh

set -eu

if [ "$#" -ne 0 ]; then
  echo "usage: $0"
  exit 1
fi

cargo build
mkdir -p extensions/vscode/out
cp target/debug/millet-ls extensions/vscode/out

cd extensions/vscode
npm run build
