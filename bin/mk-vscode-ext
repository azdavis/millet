#!/bin/sh

set -eu

if [ "$#" -ne 0 ]; then
  echo "usage: $0"
  exit 1
fi

OUT="extensions/vscode/out"

cargo build
mkdir -p "$OUT"
rm -rf "$OUT/millet-ls-impl"
cp target/debug/millet-ls "$OUT"

cd extensions/vscode
npm run build
