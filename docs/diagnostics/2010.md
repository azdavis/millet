# 2010

A name started with an underscore.

```sml
val _foo = 3
(** ^^^^ name cannot start with a `_` *)
```

## To fix

Change the name, perhaps by removing the leading underscore.

```sml
val foo = 3
```
