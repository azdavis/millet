# 5020

In an exception copy declaration, the right-hand side was not an exception.

```sml
val e = 3
exception Nope = e
(** + expected an exception, found a value *)
```

## To fix

Only use exceptions on the right-hand side.

```sml
exception E
exception Nope = E
```
