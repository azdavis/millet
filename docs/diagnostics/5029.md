# 5029

There was an unused variable.

```sml
fun ignoreArg x = 3
(**           ^ unused value: `x` *)
```

To fix, use the variable, or do not define it.

```sml
fun useArg x = x + 3
fun doNotBindArg _ = 3
```
