# 4006

There were multiple `...` rest pattern rows.

```sml
val {a, ..., ...} = {a = 1, b = "hi"}
(** ^^^^^^^^^^^^^ multiple `...` *)
```

To fix, only provide one such row.

```sml
val {a, ...} = {a = 1, b = "hi"}
```
